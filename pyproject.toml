[project]
name = "crossborder-price-tracker"
version = "1.0.0"
description = "跨境电商多平台价格追踪器"
requires-python = ">=3.9"
dependencies = ["httpx[socks]>=0.27.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.ruff]
target-version = "py39"
line-length = 100
